# Get a token at https://www.mapbox.com/studio/account/tokens/
# Copy this file to .env and fill it in — SolidStart follows Vite's convention, so VITE_-prefixed
# vars (unlike Astro's PUBLIC_ prefix) are what's exposed to client-side code.
VITE_MAPBOX_ACCESS_TOKEN=
